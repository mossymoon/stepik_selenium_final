[pytest]
markers =
    smoke: marker for smoke tests
    regression: marker for regression tests
    login_guest: marker for guest on the main page
    register_user: marker for registred user on the product page
    need_review: marker for the tests that should be reviewed